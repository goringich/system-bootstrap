# /* ---- 💫 https://github.com/JaKooLit 💫 ---- */  #
# User startup additions only.
# Base startup commands are already loaded from ~/.config/hypr/configs/Startup_Apps.conf.
# Keep this file lean to avoid duplicate daemons and extra heat/load.

$scriptsDir = $HOME/.config/hypr/scripts
$UserScripts = $HOME/.config/hypr/UserScripts

# Extra tray/panel apps not enabled in vendor defaults
exec-once = rog-control-center
exec-once = waybar

# --- app placement ---
# Cold-start desktop layout. Manual restore stays on SUPER+CTRL+ALT+R.
exec-once = bash -lc "sleep 10; hyprctl dispatch exec '[workspace 1 silent] $HOME/.local/bin/google-chrome-stable --restore-last-session'"
exec-once = bash -lc "sleep 12; hyprctl dispatch exec '[workspace 2 silent] sh -lc '\''/app/bin/code || code || code-oss || codium'\'''"
exec-once = bash -lc "sleep 14; hyprctl dispatch exec '[workspace 3 silent] sh -lc '\''Telegram || telegram-desktop || telegram'\'''"
exec-once = bash -lc "sleep 16; hyprctl dispatch exec '[workspace 4 silent] sh -lc '\''obsidian || flatpak run md.obsidian.Obsidian'\'''"
exec-once = bash -lc "sleep 18; $HOME/.config/hypr/scripts/fun.sh"
exec-once = bash -lc "sleep 11; $UserScripts/SessionAutoSave.sh"

# Wallet/keyring and local networking helper
exec-once = /usr/bin/gnome-keyring-daemon --start --components=secrets
exec-once = bash -c "sleep 3; /home/goringich/.local/bin/disable-docker-bridges.sh"

# Keep NVIDIA fans active to avoid long passive heat spikes at idle/light load.
exec-once = bash -c "sleep 4; $HOME/.local/bin/nvidia-fan-mode.sh manual 35"
