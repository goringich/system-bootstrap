#!/usr/bin/env bash
set -euo pipefail

script="${HOME}/__home_organized/scripts/system-self-check.sh"

if [[ ! -x "${script}" ]]; then
  echo "Missing diagnostic script: ${script}"
  exit 1
fi

if sudo -n true 2>/dev/null; then
  exec sudo -n bash "${script}"
fi

exec bash "${script}"
